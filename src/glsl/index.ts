// File generated by build.ts. Do not edit directly.
// This file provides all the shader code in a statically importable form, used for testing.
export const GLSLModules = {
  "fragment/pbr": "// https://www.shadertoy.com/view/XlKSDR\n\nfloat PI = 3.141592;\nfloat F_DIELECTRIC = 0.04;\n\nfloat saturate(float x) {\n  return max(x, 0.0);\n}\n\nfloat pow5(float x) {\n  float x2 = x * x;\n  return x2 * x2 * x;\n}\n\n// D - Normal distribution term\nfloat ndfGGX2(float cosTheta, float alpha) {\n  float alphaSqr = alpha * alpha;\n  float denom = cosTheta * cosTheta * (alphaSqr - 1.0) + 1.0f;\n  return alphaSqr / (PI * denom * denom);\n}\n\nfloat ndfGGX(float cosTheta, float alpha) {\n  float oneMinus = 1.0 - cosTheta * cosTheta;\n  float a = cosTheta * alpha;\n  float k = alpha / (oneMinus + a * a);\n  float d = k * k * (1.0 / PI);\n  return d;\n}\n\n// F - Schlick approximation of Fresnel\nvec3 fresnelSchlick(float cosTheta, vec3 F0) {\n  float FT = pow5(1.0f - cosTheta);\n  return F0 + (1.0 - F0) * FT;\n}\n\nfloat fresnelSchlick(float cosTheta, float f0, float f90) {\n  return f0 + (f90 - f0) * pow(1.0 - cosTheta, 5.0);\n}\n\nfloat fdBurley(float dotNL, float dotNV, float dotLH, float alpha) {\n  float f90 = 0.5 + 2.0 * alpha * dotLH * dotLH;\n  float lightScatter = fresnelSchlick(dotNL, 1.0, f90);\n  float viewScatter = fresnelSchlick(dotNV, 1.0, f90);\n  return lightScatter * viewScatter * (1.0 / PI);\n}\n\n// G - Geometric attenuation term\nfloat G1X(float dotNX, float k) {\n  return 1.0f / (dotNX * (1.0f - k) + k);\n}\n\nfloat smithGGXCorrelated(float dotNL, float dotNV, float alpha) {\n  float a2 = alpha * alpha;\n  float GGXL = dotNV * sqrt((dotNL - a2 * dotNL) * dotNL + a2);\n  float GGXV = dotNL * sqrt((dotNV - a2 * dotNV) * dotNV + a2);\n  return 0.5 / (GGXL + GGXV);\n}\n\nfloat geometricGGX(float dotNL, float dotNV, float alpha) {\n  float k = alpha / 2.0f;\n  return G1X(dotNL, k) * G1X(dotNV, k);\n}\n\n// N, L, V must be normalized\n#pragma export\nvec3 PBR(vec3 N, vec3 L, vec3 V, vec3 albedo, float metalness, float roughness) {\n\n  vec3 diffuseColor = albedo * (1.0 - metalness);\n  vec3 F0 = mix(vec3(F_DIELECTRIC), albedo, metalness);\n\n  float alpha = roughness * roughness;\n  float dotNV = saturate(dot(N, V));\n\n  float radiance = 3.1415;\n\n  vec3 H = normalize(V + L);\n  float dotNL = saturate(dot(N, L));\n  float dotNH = saturate(dot(N, H));\n  float dotLH = saturate(dot(L, H));\n\n  vec3 F = fresnelSchlick(dotLH, F0);\n  float D = ndfGGX(dotNH, alpha);\n  float G = smithGGXCorrelated(dotNL, dotNV, alpha);\n  //float G2 = geometricGGX(dotNL, dotNV, alpha);\n  //return vec3(abs(G - G2) / 100.0);\n  \n  vec3 Fd = albedo * fdBurley(dotNL, dotNV, dotLH, alpha);\n  vec3 Fs = F * D * G;\n\n  vec3 direct = (Fd + Fs) * radiance * dotNL;\n  return direct;\n}\n",
  "@use-gpu/glsl/fragment/pbr": "// https://www.shadertoy.com/view/XlKSDR\n\nfloat PI = 3.141592;\nfloat F_DIELECTRIC = 0.04;\n\nfloat saturate(float x) {\n  return max(x, 0.0);\n}\n\nfloat pow5(float x) {\n  float x2 = x * x;\n  return x2 * x2 * x;\n}\n\n// D - Normal distribution term\nfloat ndfGGX2(float cosTheta, float alpha) {\n  float alphaSqr = alpha * alpha;\n  float denom = cosTheta * cosTheta * (alphaSqr - 1.0) + 1.0f;\n  return alphaSqr / (PI * denom * denom);\n}\n\nfloat ndfGGX(float cosTheta, float alpha) {\n  float oneMinus = 1.0 - cosTheta * cosTheta;\n  float a = cosTheta * alpha;\n  float k = alpha / (oneMinus + a * a);\n  float d = k * k * (1.0 / PI);\n  return d;\n}\n\n// F - Schlick approximation of Fresnel\nvec3 fresnelSchlick(float cosTheta, vec3 F0) {\n  float FT = pow5(1.0f - cosTheta);\n  return F0 + (1.0 - F0) * FT;\n}\n\nfloat fresnelSchlick(float cosTheta, float f0, float f90) {\n  return f0 + (f90 - f0) * pow(1.0 - cosTheta, 5.0);\n}\n\nfloat fdBurley(float dotNL, float dotNV, float dotLH, float alpha) {\n  float f90 = 0.5 + 2.0 * alpha * dotLH * dotLH;\n  float lightScatter = fresnelSchlick(dotNL, 1.0, f90);\n  float viewScatter = fresnelSchlick(dotNV, 1.0, f90);\n  return lightScatter * viewScatter * (1.0 / PI);\n}\n\n// G - Geometric attenuation term\nfloat G1X(float dotNX, float k) {\n  return 1.0f / (dotNX * (1.0f - k) + k);\n}\n\nfloat smithGGXCorrelated(float dotNL, float dotNV, float alpha) {\n  float a2 = alpha * alpha;\n  float GGXL = dotNV * sqrt((dotNL - a2 * dotNL) * dotNL + a2);\n  float GGXV = dotNL * sqrt((dotNV - a2 * dotNV) * dotNV + a2);\n  return 0.5 / (GGXL + GGXV);\n}\n\nfloat geometricGGX(float dotNL, float dotNV, float alpha) {\n  float k = alpha / 2.0f;\n  return G1X(dotNL, k) * G1X(dotNV, k);\n}\n\n// N, L, V must be normalized\n#pragma export\nvec3 PBR(vec3 N, vec3 L, vec3 V, vec3 albedo, float metalness, float roughness) {\n\n  vec3 diffuseColor = albedo * (1.0 - metalness);\n  vec3 F0 = mix(vec3(F_DIELECTRIC), albedo, metalness);\n\n  float alpha = roughness * roughness;\n  float dotNV = saturate(dot(N, V));\n\n  float radiance = 3.1415;\n\n  vec3 H = normalize(V + L);\n  float dotNL = saturate(dot(N, L));\n  float dotNH = saturate(dot(N, H));\n  float dotLH = saturate(dot(L, H));\n\n  vec3 F = fresnelSchlick(dotLH, F0);\n  float D = ndfGGX(dotNH, alpha);\n  float G = smithGGXCorrelated(dotNL, dotNV, alpha);\n  //float G2 = geometricGGX(dotNL, dotNV, alpha);\n  //return vec3(abs(G - G2) / 100.0);\n  \n  vec3 Fd = albedo * fdBurley(dotNL, dotNV, dotLH, alpha);\n  vec3 Fs = F * D * G;\n\n  vec3 direct = (Fd + Fs) * radiance * dotNL;\n  return direct;\n}\n",
  "geometry/line": "#pragma import {viewUniforms} from '@use-gpu/glsl/use/view'\n\n// segments\n//\n// o--o--o  o--o--o--o  o--o\n// 1  3  2  1  3  3  2  1  2\n\nvec2 turn(vec2 xy) {\n  return vec2(xy.y, -xy.x);\n}\n\nvec2 slerp(float d, vec2 a, vec2 b, float t) {\n  float th = acos(d);\n  vec2 ab = sin(vec2((1.0 - t) * th, t * th));\n  return normalize(a * ab.x + b * ab.y);\n}\n\n#pragma export\nvec2 lineJoinBevel(vec2 left, vec2 right, int segment, float arc) {\n  if (arc > 0) return right;\n  return left;\n}\n\n#pragma export\nvec2 lineJoinMiter(vec2 left, vec2 right, int segment, float arc) {\n  vec2 mid;\n  float scale = 1.0;\n\n  if (arc == 0.0) return left;\n  if (arc == 1.0) return right;\n\n  mid = normalize((left + right) / 2.0);\n  scale = min(2.0, 1.0 / max(0.001, dot(mid, left)));\n\n  return mid * scale;\n}\n\n#pragma export\nvec2 lineJoinRound(vec2 left, vec2 right, int segment, float arc) {\n  vec2 mid;\n\n  if (arc == 0.0) return left;\n  if (arc == 1.0) return right;\n\n  float d = dot(left, right);\n  if (d > 0.999) return left;\n  return slerp(d, left, right, arc);\n  /*\n  if (d < -0.999) {\n    mid = normalize(turn(left) + turn(-right));\n    if (t < 0.5) return slerp(0.0, left, mid, t*2.0);\n    else return slerp(0.0, mid, right, t*2.0 - 1.0);\n  }\n  */\n\n  //return normalize(mix(left, right, t));\n}\n\n#pragma export\nvec3 getLineJoin(vec3 beforePoint, vec3 centerPoint, vec3 afterPoint, float arc, float y, float size, int segment, int style) {\n  vec2 before = beforePoint.xy * viewUniforms.viewSize;\n  vec2 center = centerPoint.xy * viewUniforms.viewSize;\n  vec2 after = afterPoint.xy * viewUniforms.viewSize;\n\n  vec2 left = turn(normalize(center - before));\n  vec2 right = turn(normalize(after - center));\n\n  vec2 mid;\n  if (segment == 2) {\n    mid = left;\n  }\n  else if (segment == 1) {\n    mid = right;\n  }\n  else {\n    float c = cross(vec3(left, 0.0), vec3(right, 0.0)).z;\n    if (c * y < 0.0) {\n      mid = lineJoinMiter(left, right, segment, arc);\n    }\n    else {\n      if (style == 0) mid = lineJoinBevel(left, right, segment, arc);\n      if (style == 1) mid = lineJoinMiter(left, right, segment, arc);\n      if (style == 2) mid = lineJoinRound(left, right, segment, arc);\n    }\n  }\n\n  vec2 offset = size * mid * y;\n  center.xy += offset;\n\n  return vec3(center * viewUniforms.viewResolution, centerPoint.z);\n}\n",
  "@use-gpu/glsl/geometry/line": "#pragma import {viewUniforms} from '@use-gpu/glsl/use/view'\n\n// segments\n//\n// o--o--o  o--o--o--o  o--o\n// 1  3  2  1  3  3  2  1  2\n\nvec2 turn(vec2 xy) {\n  return vec2(xy.y, -xy.x);\n}\n\nvec2 slerp(float d, vec2 a, vec2 b, float t) {\n  float th = acos(d);\n  vec2 ab = sin(vec2((1.0 - t) * th, t * th));\n  return normalize(a * ab.x + b * ab.y);\n}\n\n#pragma export\nvec2 lineJoinBevel(vec2 left, vec2 right, int segment, float arc) {\n  if (arc > 0) return right;\n  return left;\n}\n\n#pragma export\nvec2 lineJoinMiter(vec2 left, vec2 right, int segment, float arc) {\n  vec2 mid;\n  float scale = 1.0;\n\n  if (arc == 0.0) return left;\n  if (arc == 1.0) return right;\n\n  mid = normalize((left + right) / 2.0);\n  scale = min(2.0, 1.0 / max(0.001, dot(mid, left)));\n\n  return mid * scale;\n}\n\n#pragma export\nvec2 lineJoinRound(vec2 left, vec2 right, int segment, float arc) {\n  vec2 mid;\n\n  if (arc == 0.0) return left;\n  if (arc == 1.0) return right;\n\n  float d = dot(left, right);\n  if (d > 0.999) return left;\n  return slerp(d, left, right, arc);\n  /*\n  if (d < -0.999) {\n    mid = normalize(turn(left) + turn(-right));\n    if (t < 0.5) return slerp(0.0, left, mid, t*2.0);\n    else return slerp(0.0, mid, right, t*2.0 - 1.0);\n  }\n  */\n\n  //return normalize(mix(left, right, t));\n}\n\n#pragma export\nvec3 getLineJoin(vec3 beforePoint, vec3 centerPoint, vec3 afterPoint, float arc, float y, float size, int segment, int style) {\n  vec2 before = beforePoint.xy * viewUniforms.viewSize;\n  vec2 center = centerPoint.xy * viewUniforms.viewSize;\n  vec2 after = afterPoint.xy * viewUniforms.viewSize;\n\n  vec2 left = turn(normalize(center - before));\n  vec2 right = turn(normalize(after - center));\n\n  vec2 mid;\n  if (segment == 2) {\n    mid = left;\n  }\n  else if (segment == 1) {\n    mid = right;\n  }\n  else {\n    float c = cross(vec3(left, 0.0), vec3(right, 0.0)).z;\n    if (c * y < 0.0) {\n      mid = lineJoinMiter(left, right, segment, arc);\n    }\n    else {\n      if (style == 0) mid = lineJoinBevel(left, right, segment, arc);\n      if (style == 1) mid = lineJoinMiter(left, right, segment, arc);\n      if (style == 2) mid = lineJoinRound(left, right, segment, arc);\n    }\n  }\n\n  vec2 offset = size * mid * y;\n  center.xy += offset;\n\n  return vec3(center * viewUniforms.viewResolution, centerPoint.z);\n}\n",
  "geometry/quad": "const ivec2 QUAD[] = {\n  ivec2(0, 0),\n  ivec2(1, 0),\n  ivec2(0, 1),\n  ivec2(1, 1),\n};\n\n#pragma export\nivec2 getQuadIndex(int vertex) {\n  return QUAD[vertex];\n}\n\n#pragma export\nvec2 getQuadUV(int vertex) {\n  return vec2(getQuadIndex(vertex));\n}\n",
  "@use-gpu/glsl/geometry/quad": "const ivec2 QUAD[] = {\n  ivec2(0, 0),\n  ivec2(1, 0),\n  ivec2(0, 1),\n  ivec2(1, 1),\n};\n\n#pragma export\nivec2 getQuadIndex(int vertex) {\n  return QUAD[vertex];\n}\n\n#pragma export\nvec2 getQuadUV(int vertex) {\n  return vec2(getQuadIndex(vertex));\n}\n",
  "geometry/strip": "#pragma export\nivec2 getStripIndex(int vertex) {\n  int x = vertex >> 1;\n  int y = vertex & 1;\n  return ivec2(x, y);\n}\n\n#pragma export\nvec2 getStripUV(int vertex) {\n  return vec2(getStripIndex(vertex));\n}\n",
  "@use-gpu/glsl/geometry/strip": "#pragma export\nivec2 getStripIndex(int vertex) {\n  int x = vertex >> 1;\n  int y = vertex & 1;\n  return ivec2(x, y);\n}\n\n#pragma export\nvec2 getStripUV(int vertex) {\n  return vec2(getStripIndex(vertex));\n}\n",
  "instance/draw/mesh": "#pragma import {worldToClip} from '@use-gpu/glsl/use/view'\n#pragma import {pickingUniforms} from '@use-gpu/glsl/use/picking';\n#pragma import {lightUniforms} from '@use-gpu/glsl/use/light';\n\nlayout(location = 0) in vec4 position;\nlayout(location = 1) in vec4 normal;\nlayout(location = 2) in vec4 color;\nlayout(location = 3) in vec2 uv;\n\n#ifdef IS_PICKING\nlayout(location = 0) out flat uint fragIndex;\n#else\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n\nlayout(location = 2) out vec3 fragNormal;\nlayout(location = 3) out vec3 fragPosition;\n#endif\n\nvoid main() {\n  int instanceIndex = gl_InstanceIndex;\n  gl_Position = worldToClip(position);\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragColor = color;\n  fragUV = uv;\n\n  fragNormal = normal.xyz;\n  fragPosition = position.xyz;\n#endif\n}\n",
  "@use-gpu/glsl/instance/draw/mesh": "#pragma import {worldToClip} from '@use-gpu/glsl/use/view'\n#pragma import {pickingUniforms} from '@use-gpu/glsl/use/picking';\n#pragma import {lightUniforms} from '@use-gpu/glsl/use/light';\n\nlayout(location = 0) in vec4 position;\nlayout(location = 1) in vec4 normal;\nlayout(location = 2) in vec4 color;\nlayout(location = 3) in vec2 uv;\n\n#ifdef IS_PICKING\nlayout(location = 0) out flat uint fragIndex;\n#else\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n\nlayout(location = 2) out vec3 fragNormal;\nlayout(location = 3) out vec3 fragPosition;\n#endif\n\nvoid main() {\n  int instanceIndex = gl_InstanceIndex;\n  gl_Position = worldToClip(position);\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragColor = color;\n  fragUV = uv;\n\n  fragNormal = normal.xyz;\n  fragPosition = position.xyz;\n#endif\n}\n",
  "instance/draw/virtual": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n\nSolidVertex getVertex(int, int);\n\n#ifdef IS_PICKING\nlayout(location = 0) out flat uint fragIndex;\n#else\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n#endif\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  SolidVertex v = getVertex(vertexIndex, instanceIndex);\n\n  gl_Position = v.position;\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragColor = v.color;\n  fragUV = v.uv;\n#endif\n}",
  "@use-gpu/glsl/instance/draw/virtual": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n\nSolidVertex getVertex(int, int);\n\n#ifdef IS_PICKING\nlayout(location = 0) out flat uint fragIndex;\n#else\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n#endif\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  SolidVertex v = getVertex(vertexIndex, instanceIndex);\n\n  gl_Position = v.position;\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragColor = v.color;\n  fragUV = v.uv;\n#endif\n}",
  "instance/draw/wireframe-strip": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {getQuadIndex} from '@use-gpu/glsl/geometry/quad'\n#pragma import {getStripIndex} from '@use-gpu/glsl/geometry/strip'\n#pragma import {getLineJoin} from '@use-gpu/glsl/geometry/line'\n\nSolidVertex getVertex(int, int);\n\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  ivec2 ij = getQuadIndex(vertexIndex);\n  vec2 xy = vec2(ij) * 2.0 - 1.0;\n\n  int n = STRIP_SEGMENTS * 2 + 1;\n  int f = instanceIndex % n;\n  int i = instanceIndex / n;\n\n  ivec2 stripIndex = getStripIndex(f);\n  int edgeIndex = stripIndex.y;\n  int triIndex = stripIndex.x;\n\n  SolidVertex a = getVertex(triIndex, i);\n  SolidVertex b = getVertex(triIndex + 1 + edgeIndex, i);\n\n  vec3 left = a.position.xyz / a.position.w;\n  vec3 right = b.position.xyz / b.position.w;\n\n  vec3 join = (ij.x > 0)\n    ? getLineJoin(left, left, right, 0.0, xy.y, 2.0, 1, 0)\n    : getLineJoin(left, right, right, 0.0, xy.y, 2.0, 2, 0);\n\n  gl_Position = vec4(join, 1.0);\n  fragColor = vec4(1.0, 1.0, 1.0, 1.0);\n  fragUV = vec2(0.0, 0.0);\n}\n",
  "@use-gpu/glsl/instance/draw/wireframe-strip": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {getQuadIndex} from '@use-gpu/glsl/geometry/quad'\n#pragma import {getStripIndex} from '@use-gpu/glsl/geometry/strip'\n#pragma import {getLineJoin} from '@use-gpu/glsl/geometry/line'\n\nSolidVertex getVertex(int, int);\n\nlayout(location = 0) out vec4 fragColor;\nlayout(location = 1) out vec2 fragUV;\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  ivec2 ij = getQuadIndex(vertexIndex);\n  vec2 xy = vec2(ij) * 2.0 - 1.0;\n\n  int n = STRIP_SEGMENTS * 2 + 1;\n  int f = instanceIndex % n;\n  int i = instanceIndex / n;\n\n  ivec2 stripIndex = getStripIndex(f);\n  int edgeIndex = stripIndex.y;\n  int triIndex = stripIndex.x;\n\n  SolidVertex a = getVertex(triIndex, i);\n  SolidVertex b = getVertex(triIndex + 1 + edgeIndex, i);\n\n  vec3 left = a.position.xyz / a.position.w;\n  vec3 right = b.position.xyz / b.position.w;\n\n  vec3 join = (ij.x > 0)\n    ? getLineJoin(left, left, right, 0.0, xy.y, 2.0, 1, 0)\n    : getLineJoin(left, right, right, 0.0, xy.y, 2.0, 2, 0);\n\n  gl_Position = vec4(join, 1.0);\n  fragColor = vec4(1.0, 1.0, 1.0, 1.0);\n  fragUV = vec2(0.0, 0.0);\n}\n",
  "instance/fragment/mesh": "#pragma import {PBR} from '@use-gpu/glsl/fragment/pbr';\n#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n#pragma import {viewUniforms} from '@use-gpu/glsl/use/view';\n#pragma import {lightUniforms} from '@use-gpu/glsl/use/light';\n\n#ifdef IS_PICKING\nlayout(location = 0) in flat uint fragIndex;\nlayout(location = 0) out uvec4 outColor;\n#else\nlayout(set = 1, binding = 0) uniform sampler s;\nlayout(set = 1, binding = 1) uniform texture2D t;\n\nlayout(location = 0) in vec4 fragColor;\nlayout(location = 1) in vec2 fragUV;\n\nlayout(location = 2) in vec3 fragNormal;\nlayout(location = 3) in vec3 fragPosition;\n\nlayout(location = 0) out vec4 outColor;\n#endif\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  vec3 fragLight = lightUniforms.lightPosition.xyz - fragPosition;\n  vec3 fragView = viewUniforms.viewPosition.xyz - fragPosition;\n\n  vec3 N = normalize(fragNormal);\n  vec3 L = normalize(fragLight);\n  vec3 V = normalize(fragView);\n\n  vec4 texColor = texture(sampler2D(t, s), fragUV);\n  vec4 inColor = fragColor * texColor;\n  if (inColor.a <= 0.0) discard;\n\n  vec3 albedo = inColor.rgb;\n  float metalness = 0.2;\n  float roughness = 0.8;\n\n  vec3 color = PBR(N, L, V, albedo, metalness, roughness) * lightUniforms.lightColor.xyz;\n  outColor = vec4(color, inColor.a);\n\n}\n#endif\n\n",
  "@use-gpu/glsl/instance/fragment/mesh": "#pragma import {PBR} from '@use-gpu/glsl/fragment/pbr';\n#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n#pragma import {viewUniforms} from '@use-gpu/glsl/use/view';\n#pragma import {lightUniforms} from '@use-gpu/glsl/use/light';\n\n#ifdef IS_PICKING\nlayout(location = 0) in flat uint fragIndex;\nlayout(location = 0) out uvec4 outColor;\n#else\nlayout(set = 1, binding = 0) uniform sampler s;\nlayout(set = 1, binding = 1) uniform texture2D t;\n\nlayout(location = 0) in vec4 fragColor;\nlayout(location = 1) in vec2 fragUV;\n\nlayout(location = 2) in vec3 fragNormal;\nlayout(location = 3) in vec3 fragPosition;\n\nlayout(location = 0) out vec4 outColor;\n#endif\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  vec3 fragLight = lightUniforms.lightPosition.xyz - fragPosition;\n  vec3 fragView = viewUniforms.viewPosition.xyz - fragPosition;\n\n  vec3 N = normalize(fragNormal);\n  vec3 L = normalize(fragLight);\n  vec3 V = normalize(fragView);\n\n  vec4 texColor = texture(sampler2D(t, s), fragUV);\n  vec4 inColor = fragColor * texColor;\n  if (inColor.a <= 0.0) discard;\n\n  vec3 albedo = inColor.rgb;\n  float metalness = 0.2;\n  float roughness = 0.8;\n\n  vec3 color = PBR(N, L, V, albedo, metalness, roughness) * lightUniforms.lightColor.xyz;\n  outColor = vec4(color, inColor.a);\n\n}\n#endif\n\n",
  "instance/fragment/solid": "#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n\nvec4 getFragment(vec4, vec2);\n\n#ifdef IS_PICKING\nlayout(location = 0) in flat uint fragIndex;\nlayout(location = 0) out uvec4 outColor;\n#else\nlayout(location = 0) in vec4 fragColor;\nlayout(location = 1) in vec2 fragUV;\n\nlayout(location = 0) out vec4 outColor;\n#endif\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  outColor = fragColor;\n  outColor.xyz *= outColor.a;\n\n  outColor = getFragment(outColor, fragUV);\n\n  if (outColor.a <= 0.0) discard;\n}\n#endif\n",
  "@use-gpu/glsl/instance/fragment/solid": "#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n\nvec4 getFragment(vec4, vec2);\n\n#ifdef IS_PICKING\nlayout(location = 0) in flat uint fragIndex;\nlayout(location = 0) out uvec4 outColor;\n#else\nlayout(location = 0) in vec4 fragColor;\nlayout(location = 1) in vec2 fragUV;\n\nlayout(location = 0) out vec4 outColor;\n#endif\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  outColor = fragColor;\n  outColor.xyz *= outColor.a;\n\n  outColor = getFragment(outColor, fragUV);\n\n  if (outColor.a <= 0.0) discard;\n}\n#endif\n",
  "instance/ui/fragment": "#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n#pragma import {viewUniforms} from '@use-gpu/glsl/use/view';\n\nvec4 getTexture(vec2);\n\nlayout(location = 0) in flat vec4 fragRectangle;\nlayout(location = 1) in flat vec4 fragRadius;\nlayout(location = 2) in vec2 fragUV;\n#ifdef IS_PICKING\nlayout(location = 3) in flat uint fragIndex;\n#else\nlayout(location = 3) in flat vec4 fragBorder;\nlayout(location = 4) in flat vec4 fragStroke;\nlayout(location = 5) in flat vec4 fragFill;\n#endif\n\nlayout(location = 0) out vec4 outColor;\n\nfloat getBoxSDF(vec4 box, vec4 radius, vec2 uv) {\n  vec2 nearest = round(uv);\n  vec2 rr = mix(radius.xw, radius.yz, nearest.x);\n  float r = mix(rr.x, rr.y, nearest.y);\n\n  vec2 wh = box.zw - box.xy;\n  vec2 xy = (abs((uv - .5)) - .5) * wh + r;\n  vec2 clip = max(vec2(0.0), xy);\n  float neg = min(0.0, max(xy.x, xy.y));\n\n  return r + 0.5 - length(clip) - neg;\n}\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  float sdf = getBoxSDF(fragRectangle, fragRadius, fragUV);\n\n  vec4 color = mix(fragFill, fragStroke, clamp(sdf - fragBorder, 0.0, 1.0));\n  outColor = color;\n\n  float mask = clamp(sdf, 0.0, 1.0);\n  outColor *= mask;\n}\n#endif\n",
  "@use-gpu/glsl/instance/ui/fragment": "#pragma import {getPickingColor} from '@use-gpu/glsl/use/picking';\n#pragma import {viewUniforms} from '@use-gpu/glsl/use/view';\n\nvec4 getTexture(vec2);\n\nlayout(location = 0) in flat vec4 fragRectangle;\nlayout(location = 1) in flat vec4 fragRadius;\nlayout(location = 2) in vec2 fragUV;\n#ifdef IS_PICKING\nlayout(location = 3) in flat uint fragIndex;\n#else\nlayout(location = 3) in flat vec4 fragBorder;\nlayout(location = 4) in flat vec4 fragStroke;\nlayout(location = 5) in flat vec4 fragFill;\n#endif\n\nlayout(location = 0) out vec4 outColor;\n\nfloat getBoxSDF(vec4 box, vec4 radius, vec2 uv) {\n  vec2 nearest = round(uv);\n  vec2 rr = mix(radius.xw, radius.yz, nearest.x);\n  float r = mix(rr.x, rr.y, nearest.y);\n\n  vec2 wh = box.zw - box.xy;\n  vec2 xy = (abs((uv - .5)) - .5) * wh + r;\n  vec2 clip = max(vec2(0.0), xy);\n  float neg = min(0.0, max(xy.x, xy.y));\n\n  return r + 0.5 - length(clip) - neg;\n}\n\n#ifdef IS_PICKING\nvoid main() {\n  outColor = getPickingColor(fragIndex);\n}\n#else\nvoid main() {\n  float sdf = getBoxSDF(fragRectangle, fragRadius, fragUV);\n\n  vec4 color = mix(fragFill, fragStroke, clamp(sdf - fragBorder, 0.0, 1.0));\n  outColor = color;\n\n  float mask = clamp(sdf, 0.0, 1.0);\n  outColor *= mask;\n}\n#endif\n",
  "instance/ui/vertex": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {worldToClip} from '@use-gpu/glsl/use/view'\n#pragma import {getQuadUV} from '@use-gpu/glsl/geometry/quad'\n\n#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n\nlayout(location = 0) out flat vec4 fragRectangle;\nlayout(location = 1) out flat vec4 fragRadius;\nlayout(location = 2) out vec2 fragUV;\n#ifdef IS_PICKING\nlayout(location = 3) out flat uint fragIndex;\n#else\nlayout(location = 3) out flat vec4 fragBorder;\nlayout(location = 4) out flat vec4 fragStroke;\nlayout(location = 5) out flat vec4 fragFill;\n#endif\n\nvec4 getRectangle(int);\nvec4 getRadius(int);\nvec4 getBorder(int);\nvec4 getFill(int);\nvec4 getStroke(int);\nvec4 getUV(int);\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  vec4 rectangle = getRectangle(instanceIndex);\n  vec4 radius = getRadius(instanceIndex);\n  vec4 border = getBorder(instanceIndex);\n  vec4 fill = getFill(instanceIndex);\n  vec4 stroke = getStroke(instanceIndex);\n  vec4 uv4 = getUV(instanceIndex);\n\n  vec2 uv = getQuadUV(vertexIndex);\n  vec4 position = vec4(mix(rectangle.xy, rectangle.zw, uv), 0.5, 1.0);\n  vec4 center = worldToClip(position);\n\n  uv = mix(uv4.xy, uv4.zw, uv);\n\n  gl_Position = center;\n\n  fragRectangle = rectangle;\n  fragRadius = radius;\n  fragUV = uv;\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragBorder = border;\n  fragStroke = stroke;\n  fragFill = fill;\n#endif\n}",
  "@use-gpu/glsl/instance/ui/vertex": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {worldToClip} from '@use-gpu/glsl/use/view'\n#pragma import {getQuadUV} from '@use-gpu/glsl/geometry/quad'\n\n#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n\nlayout(location = 0) out flat vec4 fragRectangle;\nlayout(location = 1) out flat vec4 fragRadius;\nlayout(location = 2) out vec2 fragUV;\n#ifdef IS_PICKING\nlayout(location = 3) out flat uint fragIndex;\n#else\nlayout(location = 3) out flat vec4 fragBorder;\nlayout(location = 4) out flat vec4 fragStroke;\nlayout(location = 5) out flat vec4 fragFill;\n#endif\n\nvec4 getRectangle(int);\nvec4 getRadius(int);\nvec4 getBorder(int);\nvec4 getFill(int);\nvec4 getStroke(int);\nvec4 getUV(int);\n\nvoid main() {\n  int vertexIndex = gl_VertexIndex;\n  int instanceIndex = gl_InstanceIndex;\n\n  vec4 rectangle = getRectangle(instanceIndex);\n  vec4 radius = getRadius(instanceIndex);\n  vec4 border = getBorder(instanceIndex);\n  vec4 fill = getFill(instanceIndex);\n  vec4 stroke = getStroke(instanceIndex);\n  vec4 uv4 = getUV(instanceIndex);\n\n  vec2 uv = getQuadUV(vertexIndex);\n  vec4 position = vec4(mix(rectangle.xy, rectangle.zw, uv), 0.5, 1.0);\n  vec4 center = worldToClip(position);\n\n  uv = mix(uv4.xy, uv4.zw, uv);\n\n  gl_Position = center;\n\n  fragRectangle = rectangle;\n  fragRadius = radius;\n  fragUV = uv;\n#ifdef IS_PICKING\n  fragIndex = uint(instanceIndex);\n#else\n  fragBorder = border;\n  fragStroke = stroke;\n  fragFill = fill;\n#endif\n}",
  "instance/vertex/line": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {worldToClip, worldToClip3D, getPerspectiveScale} from '@use-gpu/glsl/use/view'\n#pragma import {getStripIndex} from '@use-gpu/glsl/geometry/strip'\n#pragma import {getLineJoin} from '@use-gpu/glsl/geometry/line'\n\nfloat NaN = 0.0/0.0;\n\nvec4 getPosition(int);\nint getSegment(int);\nvec4 getColor(int);\nfloat getSize(int);\nfloat getDepth(int);\n\n#pragma export\nSolidVertex getLineVertex(int vertexIndex, int instanceIndex) {\n  ivec2 ij = getStripIndex(vertexIndex);\n\n  int segmentLeft = getSegment(instanceIndex);\n  if (segmentLeft == 0 || segmentLeft == 2) {\n    return SolidVertex(\n      vec4(NaN, NaN, NaN, NaN),\n      vec4(NaN, NaN, NaN, NaN),\n      vec2(NaN, NaN)\n    );\n  }\n\n  vec2 uv = vec2(ij);\n  vec2 xy = uv * 2.0 - 1.0;\n\n  int cornerIndex, joinIndex;\n  if (ij.x == 0) {\n    joinIndex = LINE_JOIN_SIZE;\n    cornerIndex = instanceIndex;\n  }\n  else {\n    joinIndex = ij.x - 1;\n    cornerIndex = instanceIndex + 1;\n  }\n\n  int segment = getSegment(cornerIndex);\n  vec4 color = getColor(cornerIndex);\n  float size = getSize(cornerIndex);\n  float depth = getDepth(cornerIndex);\n\n  vec4 beforePos = getPosition(cornerIndex - 1);\n  vec4 centerPos = getPosition(cornerIndex);\n  vec4 afterPos = getPosition(cornerIndex + 1);\n\n  vec3 before = worldToClip3D(beforePos);\n  vec4 center = worldToClip(centerPos);\n  vec3 after = worldToClip3D(afterPos);\n\n  // Lerp between fixed size and full perspective\n  float pixelScale = getPerspectiveScale(center.w, depth);\n  size *= pixelScale;\n\n  float arc = joinIndex / float(LINE_JOIN_SIZE);\n  vec3 lineJoin = getLineJoin(before, center.xyz / center.w, after, arc, xy.y, size, segment, LINE_JOIN_STYLE);\n\n  return SolidVertex(\n    vec4(lineJoin, 1.0),\n    color,\n    uv\n  );\n}\n",
  "@use-gpu/glsl/instance/vertex/line": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {worldToClip, worldToClip3D, getPerspectiveScale} from '@use-gpu/glsl/use/view'\n#pragma import {getStripIndex} from '@use-gpu/glsl/geometry/strip'\n#pragma import {getLineJoin} from '@use-gpu/glsl/geometry/line'\n\nfloat NaN = 0.0/0.0;\n\nvec4 getPosition(int);\nint getSegment(int);\nvec4 getColor(int);\nfloat getSize(int);\nfloat getDepth(int);\n\n#pragma export\nSolidVertex getLineVertex(int vertexIndex, int instanceIndex) {\n  ivec2 ij = getStripIndex(vertexIndex);\n\n  int segmentLeft = getSegment(instanceIndex);\n  if (segmentLeft == 0 || segmentLeft == 2) {\n    return SolidVertex(\n      vec4(NaN, NaN, NaN, NaN),\n      vec4(NaN, NaN, NaN, NaN),\n      vec2(NaN, NaN)\n    );\n  }\n\n  vec2 uv = vec2(ij);\n  vec2 xy = uv * 2.0 - 1.0;\n\n  int cornerIndex, joinIndex;\n  if (ij.x == 0) {\n    joinIndex = LINE_JOIN_SIZE;\n    cornerIndex = instanceIndex;\n  }\n  else {\n    joinIndex = ij.x - 1;\n    cornerIndex = instanceIndex + 1;\n  }\n\n  int segment = getSegment(cornerIndex);\n  vec4 color = getColor(cornerIndex);\n  float size = getSize(cornerIndex);\n  float depth = getDepth(cornerIndex);\n\n  vec4 beforePos = getPosition(cornerIndex - 1);\n  vec4 centerPos = getPosition(cornerIndex);\n  vec4 afterPos = getPosition(cornerIndex + 1);\n\n  vec3 before = worldToClip3D(beforePos);\n  vec4 center = worldToClip(centerPos);\n  vec3 after = worldToClip3D(afterPos);\n\n  // Lerp between fixed size and full perspective\n  float pixelScale = getPerspectiveScale(center.w, depth);\n  size *= pixelScale;\n\n  float arc = joinIndex / float(LINE_JOIN_SIZE);\n  vec3 lineJoin = getLineJoin(before, center.xyz / center.w, after, arc, xy.y, size, segment, LINE_JOIN_STYLE);\n\n  return SolidVertex(\n    vec4(lineJoin, 1.0),\n    color,\n    uv\n  );\n}\n",
  "instance/vertex/quad": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {viewUniforms, worldToClip, getPerspectiveScale} from '@use-gpu/glsl/use/view'\n#pragma import {getQuadUV} from '@use-gpu/glsl/geometry/quad'\n\nvec4 getPosition(int);\nvec4 getColor(int);\nvec2 getSize(int);\nfloat getDepth(int);\n\n#pragma export\nSolidVertex getQuadVertex(int vertexIndex, int instanceIndex) {\n  vec4 position = getPosition(instanceIndex);\n  vec4 color = getColor(instanceIndex);\n  vec2 size = getSize(instanceIndex);\n  float depth = getDepth(instanceIndex);\n\n  vec4 center = worldToClip(position);\n\n  vec2 uv = getQuadUV(vertexIndex);\n  vec2 xy = uv * 2.0 - 1.0;\n  \n  // Lerp between fixed size and full perspective.\n  float pixelScale = getPerspectiveScale(center.w, depth);\n  size *= pixelScale;\n\n  #ifdef HAS_EDGE_BLEED\n  xy = xy * (size + 0.5) / size;\n  uv = xy * .5 + .5;\n  #endif\n\n  center.xy += xy * size * viewUniforms.viewResolution * center.w;\n\n  return SolidVertex(\n    center,\n    color,\n    uv\n  );\n}",
  "@use-gpu/glsl/instance/vertex/quad": "#pragma import {SolidVertex} from '@use-gpu/glsl/use/types'\n#pragma import {viewUniforms, worldToClip, getPerspectiveScale} from '@use-gpu/glsl/use/view'\n#pragma import {getQuadUV} from '@use-gpu/glsl/geometry/quad'\n\nvec4 getPosition(int);\nvec4 getColor(int);\nvec2 getSize(int);\nfloat getDepth(int);\n\n#pragma export\nSolidVertex getQuadVertex(int vertexIndex, int instanceIndex) {\n  vec4 position = getPosition(instanceIndex);\n  vec4 color = getColor(instanceIndex);\n  vec2 size = getSize(instanceIndex);\n  float depth = getDepth(instanceIndex);\n\n  vec4 center = worldToClip(position);\n\n  vec2 uv = getQuadUV(vertexIndex);\n  vec2 xy = uv * 2.0 - 1.0;\n  \n  // Lerp between fixed size and full perspective.\n  float pixelScale = getPerspectiveScale(center.w, depth);\n  size *= pixelScale;\n\n  #ifdef HAS_EDGE_BLEED\n  xy = xy * (size + 0.5) / size;\n  uv = xy * .5 + .5;\n  #endif\n\n  center.xy += xy * size * viewUniforms.viewResolution * center.w;\n\n  return SolidVertex(\n    center,\n    color,\n    uv\n  );\n}",
  "mask/masked": "float getMask(vec2);\nvec4 getTexture(vec2);\n\n#pragma export\nvec4 getMaskedFragment(vec4 color, vec2 uv) {\n  color *= getMask(uv);\n  color *= getTexture(uv);\n  return color;\n}",
  "@use-gpu/glsl/mask/masked": "float getMask(vec2);\nvec4 getTexture(vec2);\n\n#pragma export\nvec4 getMaskedFragment(vec4 color, vec2 uv) {\n  color *= getMask(uv);\n  color *= getTexture(uv);\n  return color;\n}",
  "mask/passthru": "#pragma export\nvec4 getPassThruFragment(vec4 color, vec2 uv) {\n  return color;\n}",
  "@use-gpu/glsl/mask/passthru": "#pragma export\nvec4 getPassThruFragment(vec4 color, vec2 uv) {\n  return color;\n}",
  "mask/point": "const float OUTLINE = 0.4;\n\nfloat getUVScale(vec2 uv) {\n  vec2 dx = dFdx(uv);\n  vec2 dy = dFdy(uv);\n  // implicit factor 2 to account for uv -> xy map\n  return length(dx) + length(dy);\n}\n\nfloat scaleSDF(float sdf, float scale) {\n  float d = sdf / scale + 0.5;\n  return clamp(d, 0.0, 1.0);\n}\n\nfloat outlineSDF(float sdf) {\n  return min(sdf, OUTLINE - sdf);\n}\n\nfloat circleSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - length(xy);\n}\n\nfloat diamondSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - (abs(xy.x) + abs(xy.y));\n}\n\nfloat squareSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - max(abs(xy.x), abs(xy.y));\n}\n\n#pragma export\nfloat circle(vec2 uv) {\n  float l = circleSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat diamond(vec2 uv) {\n  float l = diamondSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat square(vec2 uv) {\n  float l = squareSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat circleOutlined(vec2 uv) {\n  float l = outlineSDF(circleSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat diamondOutlined(vec2 uv) {\n  float l = outlineSDF(diamondSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat squareOutlined(vec2 uv) {\n  float l = outlineSDF(squareSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n",
  "@use-gpu/glsl/mask/point": "const float OUTLINE = 0.4;\n\nfloat getUVScale(vec2 uv) {\n  vec2 dx = dFdx(uv);\n  vec2 dy = dFdy(uv);\n  // implicit factor 2 to account for uv -> xy map\n  return length(dx) + length(dy);\n}\n\nfloat scaleSDF(float sdf, float scale) {\n  float d = sdf / scale + 0.5;\n  return clamp(d, 0.0, 1.0);\n}\n\nfloat outlineSDF(float sdf) {\n  return min(sdf, OUTLINE - sdf);\n}\n\nfloat circleSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - length(xy);\n}\n\nfloat diamondSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - (abs(xy.x) + abs(xy.y));\n}\n\nfloat squareSDF(vec2 uv) {\n  vec2 xy = uv * 2.0 - 1.0;\n  return 1.0 - max(abs(xy.x), abs(xy.y));\n}\n\n#pragma export\nfloat circle(vec2 uv) {\n  float l = circleSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat diamond(vec2 uv) {\n  float l = diamondSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat square(vec2 uv) {\n  float l = squareSDF(uv);\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat circleOutlined(vec2 uv) {\n  float l = outlineSDF(circleSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat diamondOutlined(vec2 uv) {\n  float l = outlineSDF(diamondSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n\n#pragma export\nfloat squareOutlined(vec2 uv) {\n  float l = outlineSDF(squareSDF(uv));\n  float s = getUVScale(uv);\n  return scaleSDF(l, s);\n}\n",
  "use/light": "//#pragma export\n//struct Light {\n//  vec4 position;\n//  vec4 color;\n//};\n\n#pragma export\nlayout(set = LIGHT_BINDGROUP, binding = LIGHT_BINDING) uniform LightUniforms {\n  vec4 lightPosition;\n  vec4 lightColor;\n} lightUniforms;\n",
  "@use-gpu/glsl/use/light": "//#pragma export\n//struct Light {\n//  vec4 position;\n//  vec4 color;\n//};\n\n#pragma export\nlayout(set = LIGHT_BINDGROUP, binding = LIGHT_BINDING) uniform LightUniforms {\n  vec4 lightPosition;\n  vec4 lightColor;\n} lightUniforms;\n",
  "use/picking": "#ifdef IS_PICKING\n#pragma export\nlayout(set = PICKING_BINDGROUP, binding = PICKING_BINDING) uniform PickingUniforms {\n  uint pickingId;\n} pickingUniforms;\n\n#pragma export\nuvec4 getPickingColor(uint fragIndex) {\n  uint r = pickingUniforms.pickingId;\n  uint g = fragIndex;\n  return uvec4(r, g, 0, 0);\n}\n#endif\n",
  "@use-gpu/glsl/use/picking": "#ifdef IS_PICKING\n#pragma export\nlayout(set = PICKING_BINDGROUP, binding = PICKING_BINDING) uniform PickingUniforms {\n  uint pickingId;\n} pickingUniforms;\n\n#pragma export\nuvec4 getPickingColor(uint fragIndex) {\n  uint r = pickingUniforms.pickingId;\n  uint g = fragIndex;\n  return uvec4(r, g, 0, 0);\n}\n#endif\n",
  "use/types": "#pragma export\nstruct SolidVertex {\n  vec4 position;\n  vec4 color;\n  vec2 uv;\n};\n\n#pragma export\nstruct MeshVertex {\n  vec4 position;\n  vec3 normal;\n  vec4 color;\n  vec2 uv;\n};",
  "@use-gpu/glsl/use/types": "#pragma export\nstruct SolidVertex {\n  vec4 position;\n  vec4 color;\n  vec2 uv;\n};\n\n#pragma export\nstruct MeshVertex {\n  vec4 position;\n  vec3 normal;\n  vec4 color;\n  vec2 uv;\n};",
  "use/view": "#pragma export\nlayout(set = VIEW_BINDGROUP, binding = VIEW_BINDING) uniform ViewUniforms {\n  mat4 projectionMatrix;\n  mat4 viewMatrix;\n  vec4 viewPosition;\n  vec2 viewResolution;\n  vec2 viewSize;\n  float viewWorldUnit;\n  float viewPixelRatio;\n} viewUniforms;\n\n#pragma export\nvec4 worldToView(vec4 position) {\n  return viewUniforms.viewMatrix * position;\n}\n\n#pragma export\nvec4 viewToClip(vec4 position) {\n  return viewUniforms.projectionMatrix * position;\n}\n\n#pragma export\nvec4 worldToClip(vec4 position) {\n  return viewToClip(worldToView(position));\n}\n\n#pragma export\nvec3 clipToScreen3D(vec4 position) {\n  return vec3(position.xy * viewUniforms.viewSize, position.z);\n}\n\n#pragma export\nvec3 screenToClip3D(vec4 position) {\n  return vec3(position.xy * viewUniforms.viewResolution, position.z);\n}\n\n#pragma export\nvec3 worldToClip3D(vec4 position) {\n  position = viewToClip(worldToView(position));\n  return position.xyz / position.w;\n}\n\n#pragma export\nfloat getPerspectiveScale(float w, float f) {\n  mat4 m = viewUniforms.projectionMatrix;\n  float worldScale = m[1][1] * viewUniforms.viewWorldUnit;\n  float clipScale = mix(1.0, worldScale / w, f);\n  float pixelScale = clipScale * viewUniforms.viewPixelRatio;\n  return pixelScale;\n}\n",
  "@use-gpu/glsl/use/view": "#pragma export\nlayout(set = VIEW_BINDGROUP, binding = VIEW_BINDING) uniform ViewUniforms {\n  mat4 projectionMatrix;\n  mat4 viewMatrix;\n  vec4 viewPosition;\n  vec2 viewResolution;\n  vec2 viewSize;\n  float viewWorldUnit;\n  float viewPixelRatio;\n} viewUniforms;\n\n#pragma export\nvec4 worldToView(vec4 position) {\n  return viewUniforms.viewMatrix * position;\n}\n\n#pragma export\nvec4 viewToClip(vec4 position) {\n  return viewUniforms.projectionMatrix * position;\n}\n\n#pragma export\nvec4 worldToClip(vec4 position) {\n  return viewToClip(worldToView(position));\n}\n\n#pragma export\nvec3 clipToScreen3D(vec4 position) {\n  return vec3(position.xy * viewUniforms.viewSize, position.z);\n}\n\n#pragma export\nvec3 screenToClip3D(vec4 position) {\n  return vec3(position.xy * viewUniforms.viewResolution, position.z);\n}\n\n#pragma export\nvec3 worldToClip3D(vec4 position) {\n  position = viewToClip(worldToView(position));\n  return position.xyz / position.w;\n}\n\n#pragma export\nfloat getPerspectiveScale(float w, float f) {\n  mat4 m = viewUniforms.projectionMatrix;\n  float worldScale = m[1][1] * viewUniforms.viewWorldUnit;\n  float clipScale = mix(1.0, worldScale / w, f);\n  float pixelScale = clipScale * viewUniforms.viewPixelRatio;\n  return pixelScale;\n}\n"
};
export default GLSLModules;
