export type Axis4 = 'x' | 'y' | 'z' | 'w';
export type Swizzle = string;

export type DomainOptions = {
  divide: number,
  unit: number,
  base: number,
  start: boolean,
  end: boolean,
  zero: boolean,
  factor: number,
  nice: boolean,
};
