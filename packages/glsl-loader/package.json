{
  "name": "@use-gpu/glsl-loader",
  "version": "0.0.1",
  "main": "src/index.ts",
  "author": "Steven Wittens <steven@acko.net>",
  "license": "MIT",
  "peerDependencies": {
    "@use-gpu/shader": "*"
  },
  "dependencies": {
    "require-hacker": "^3.0.1"
  },
  "bin": {
    "glsl-tsgen": "./bin/glsl-tsgen"
  },
  "scripts": {
    "test": "jest --watch",
    "lint": "eslint src --ext ts",
    "coverage": "echo 'jest --collect-coverage' disabled",
    "types": "tsc --noEmit",
    "types:watch": "yarn run types -- --watch",
    "build": "yarn run build:types && yarn run build:js && yarn run build:bin && yarn run build:pkg",
    "build:types": "rimraf tsconfig.tsbuildinfo && tsc --emitDeclarationOnly",
    "build:js": "babel src --out-dir ../../build/packages/glsl-loader/src --extensions \".ts\" --ignore \"src/**/__mocks__/**/*.js\" --ignore \"src/**/*.test.ts\"; babel scripts --out-dir ../../build/packages/glsl-loader/scripts --extensions \".ts\"; babel node.ts --out-dir ../../build/packages/glsl-loader --extensions \".ts\"",
    "build:bin": "mkdir ../../build/packages/glsl-loader/bin 2>/dev/null; cp bin/glsl-tsgen ../../build/packages/glsl-loader/bin",
    "build:pkg": "cp package.json ../../build/packages/glsl-loader"
  },
  "devDependencies": {
    "@types/loader-utils": "^2.0.3",
    "@types/lodash": "^4.14.176",
    "@types/require-hacker": "^3.0.1"
  }
}
