{
  "name": "@use-gpu/app",
  "version": "0.0.1",
  "main": "src/index.ts",
  "author": "Steven Wittens <steven@acko.net>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@use-gpu/live": "*",
    "@use-gpu/core": "*",
    "@use-gpu/webgpu": "*",
    "@use-gpu/components": "*"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "jest --watch",
    "lint": "eslint src --ext ts",
    "coverage": "echo 'jest --collect-coverage' disabled",
    "types": "tsc --noEmit",
    "types:watch": "yarn run types -- --watch",

    "build": "yarn run build:types && yarn run build:js && yarn run build:pkg",
    "build:types": "rimraf tsconfig.tsbuildinfo && tsc --emitDeclarationOnly",
    "build:js": "babel src --out-dir ../../build/packages/app/src --extensions \".ts\" --ignore \"src/**/__mocks__/**/*.js\" --ignore \"src/**/*.test.ts\"",
    "build:pkg": "cp package.json ../../build/packages/app"
  }
}
